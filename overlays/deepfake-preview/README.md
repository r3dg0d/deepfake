# deepfake-preview

Matrix-themed Quickshell overlay for `deepfake`.

```bash
deepfake webcam --source=face.jpg --consent-ack
deepfake-preview   # overlay only
```

`DEEPFAKE_QS_PREVIEW=0` disables auto-start. `DEEPFAKE_OPENCV_PREVIEW=1` also tries OpenCV windows.
