# Contributing

- Python 3.11+.
- `pip install -e ".[dev]" && pytest`
- Keep consent gates and default watermark behavior.
- Never add silent large model downloads; never vendor giant weights into git.
- Do not illegally relicense AlphaFace (MIT) or claim Drive weights are redistributable.
- InsightFace fallback must keep NON-COMMERCIAL warnings.
- MIT for contributions to *this* wrapper.
